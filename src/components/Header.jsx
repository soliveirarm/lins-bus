import { BusIcon } from "./BusIcon"

export const Header = () => (
  <header className="flex items-center gap-4 p-4 text-accent">
    <BusIcon />
    <h1 className="font-bold text-2xl">Lins Bus</h1>
  </header>
)
